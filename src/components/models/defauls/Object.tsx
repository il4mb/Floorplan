import { Box } from "lucide-react";
import { Model } from "@/utils/model";

export default {

    type: "object",
    name: "Object",
    icon: (props) => <Box {...props} />,
    render({ }) {

        return (
            <>

            </>
        )
    }
} as Model
